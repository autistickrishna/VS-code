# VS code

This repository is a copy of the `VS code` folder from the local machine: `Desktop/cpp_projects/VS code`.

Quick start

- Build (C++):

  - Compile the `New.c++` file with clang++:

    clang++ -g "New.c++" -o New

  - Run:

    ./New

Repository contents

- `New.c++` — example source file from your workspace.

Notes

- This repo was created and pushed from the local machine. Feel free to rename, make private, or add a README section describing the project.
